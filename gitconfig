[user]
name = Luke Opperman
email = loppear@gmail.com

[core]
excludesfile = ~/etc/hgglobalignore

[format]
pretty=short

[alias]
lol = log --abbrev-commit --graph --decorate --relative-date
ll = log --abbrev-commit --graph --decorate --relative-date --all --date-order
lls = log --abbrev-commit --graph --decorate --relative-date --all --simplify-by-decoration
st = status
ci = commit -a
co = checkout
br = branch
di = diff
dc = diff --cached
up = fetch --all

[push]
default = upstream

[color]
ui = auto

[merge]
tool = kdiff3
defaultToUpstream = true
